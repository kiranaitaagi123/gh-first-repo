name: First Workflow
on:
  workflow_dispatch:

jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - name: Step 1
        run: echo "Hello from First Workflow!"
      
      - name: Step 2
        run: echo "First workflow is completing..."
